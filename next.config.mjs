/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com', 'images.pexels.com', 'www.pexels.com', 'media.licdn.com'], // Add more domains as needed
    },
  };
  
  export default nextConfig;
  